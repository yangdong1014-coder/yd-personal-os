# systemd reads this file itself; it is not a shell script.
# It contains release approval metadata only. Runtime secrets and the database
# path come from the active descriptor's separately protected config/artifacts.
PSY_APPROVED_APP_VERSION=v2.2.0
PSY_APPROVED_GIT_COMMIT=replace_with_approved_40_character_lowercase_commit
